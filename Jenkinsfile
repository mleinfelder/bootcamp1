pipeline {
  agent any
  stages {
    stage('Docker build') {
      steps {
        sh '''stage(\'Docker Build\') { // for display purposes
      // Get some code from a GitHub repository
      sh "sudo docker build -t myapp ."
      
   }'''
      }
    }

  }
}